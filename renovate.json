{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Renovate disabled — retired/archived downstream mirror of myportfolio (ADR 0024). All dependency management lives in mp (single source); do not re-enable.",
  "enabled": false
}