import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Typography" />

# Typography

Five font families, a modular scale, and six heading levels create clear
hierarchy. Migrated from `myportfolio/app/app/design-system/typography/page.tsx`
(Wave J, May 2026).

## Font Families

Loaded via `next/font/local` in `app/layout.tsx`. The seed token tier in
`nectar-design` declares portable fallbacks for downstream consumers —
those aren't what's on screen. The override is the source of truth.

| Slot | Family | Role |
| --- | --- | --- |
| `font-display` | **Google Sans Flex** (1–1000 wght) | Hero headings, big numbers |
| `font-sans` | **Plus Jakarta Sans** (200–800, italic) | Body, UI, default |
| `font-serif` | **Playfair Display** (400–900, italic) | Display serif, accents (e.g. hero italic emphasis) |
| `font-caption` | **Crimson Pro** (200–900, italic) | Pull quotes, captions, asides |
| `font-mono` | **Monaspace Neon** | Code, technical labels |

All five are self-hosted as woff2 under `app/fonts/`. The seed table in
`nectar-design/tokens` lists fallback families (Switzer / Libre Baskerville
/ Merriweather / Roboto Mono) for downstream consumers — not what's loaded
in the live site.

### Family specimens

<div style={{ display: 'grid', gap: 12, margin: '12px 0 24px' }}>
  {[
    { slot: 'font-display', name: 'Google Sans Flex', sample: 'A designer learning to ship', css: { fontFamily: 'var(--font-display, ui-sans-serif)', fontWeight: 800, fontSize: 28, letterSpacing: '-0.02em' } },
    { slot: 'font-sans',    name: 'Plus Jakarta Sans', sample: 'Body, UI, default — the workhorse of the system.', css: { fontFamily: 'var(--font-sans, ui-sans-serif)', fontWeight: 400, fontSize: 15, lineHeight: 1.55 } },
    { slot: 'font-serif',   name: 'Playfair Display',  sample: 'Learning the engineering', css: { fontFamily: 'var(--font-serif, ui-serif)', fontStyle: 'italic', fontWeight: 500, fontSize: 26 } },
    { slot: 'font-caption', name: 'Crimson Pro',       sample: '"A pull quote, an aside, a moment."', css: { fontFamily: 'var(--font-caption, ui-serif)', fontStyle: 'italic', fontWeight: 400, fontSize: 18, lineHeight: 1.4 } },
    { slot: 'font-mono',    name: 'Monaspace Neon',    sample: 'const heat = useHeatEngine();', css: { fontFamily: 'var(--font-mono, ui-monospace)', fontSize: 14, letterSpacing: '0.02em' } },
  ].map((f) => (
    <div
      key={f.slot}
      style={{
        padding: '14px 16px',
        borderRadius: 12,
        background: 'oklch(var(--L-surface) 0.02 var(--dynamic-hue) / 0.4)',
        border: '1px solid oklch(var(--L-heading) 0.01 var(--dynamic-hue) / 0.08)',
      }}
    >
      <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 8, fontFamily: 'var(--font-mono, ui-monospace)', fontSize: 11, color: 'oklch(var(--L-muted) 0.03 var(--dynamic-hue))' }}>
        <code>{f.slot}</code>
        <span>{f.name}</span>
      </div>
      <div style={{ ...f.css, color: 'oklch(var(--L-heading) 0.02 var(--dynamic-hue))' }}>{f.sample}</div>
    </div>
  ))}
</div>

## Type Scale

Major third scale (1.25 ratio). Sizes reference seed tokens via CSS custom
properties.

| Token | Pixel | Role |
| --- | --- | --- |
| `4xl` | ~61px | H1 |
| `3xl` | ~49px | H2 |
| `2xl` | ~39px | H3 |
| `xl` | ~31px | H4 |
| `lg` | ~25px | H5 |
| `md` | ~20px | H6 |
| `base` | 16px | Body |
| `sm` | ~13px | Small |
| `xs` | ~10px | Fine |

### Scale ladder

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'auto 1fr',
    columnGap: 24,
    rowGap: 14,
    alignItems: 'baseline',
    margin: '16px 0 24px',
    padding: '20px 24px',
    borderRadius: 12,
    background: 'oklch(var(--L-surface) 0.02 var(--dynamic-hue) / 0.4)',
    border: '1px solid oklch(var(--L-heading) 0.01 var(--dynamic-hue) / 0.08)',
  }}
>
  {[
    { token: '4xl', px: 61, label: 'H1',     css: { fontFamily: 'var(--font-display)', fontWeight: 900, letterSpacing: '-0.02em', fontSize: 61 } },
    { token: '3xl', px: 49, label: 'H2',     css: { fontFamily: 'var(--font-display)', fontWeight: 800, letterSpacing: '-0.02em', fontSize: 49 } },
    { token: '2xl', px: 39, label: 'H3',     css: { fontFamily: 'var(--font-display)', fontWeight: 700, letterSpacing: '-0.015em', fontSize: 39 } },
    { token: 'xl',  px: 31, label: 'H4',     css: { fontFamily: 'var(--font-sans)', fontWeight: 700, fontSize: 31 } },
    { token: 'lg',  px: 25, label: 'H5',     css: { fontFamily: 'var(--font-sans)', fontWeight: 600, fontSize: 25 } },
    { token: 'md',  px: 20, label: 'H6',     css: { fontFamily: 'var(--font-sans)', fontWeight: 600, fontSize: 20 } },
    { token: 'base', px: 16, label: 'Body',  css: { fontFamily: 'var(--font-sans)', fontWeight: 400, fontSize: 16, lineHeight: 1.55 } },
    { token: 'sm',  px: 13, label: 'Small',  css: { fontFamily: 'var(--font-sans)', fontWeight: 400, fontSize: 13 } },
    { token: 'xs',  px: 10, label: 'Fine',   css: { fontFamily: 'var(--font-mono)', fontSize: 10, textTransform: 'uppercase', letterSpacing: '0.14em' } },
  ].flatMap((row) => [
    <code key={`${row.token}-tag`} style={{ fontFamily: 'var(--font-mono, ui-monospace)', fontSize: 11, color: 'oklch(var(--L-muted) 0.03 var(--dynamic-hue))', whiteSpace: 'nowrap' }}>
      {row.token} · {row.px}px
    </code>,
    <div key={`${row.token}-sample`} style={{ ...row.css, color: 'oklch(var(--L-heading) 0.02 var(--dynamic-hue))' }}>{row.label}</div>,
  ])}
</div>

Plus three fluid sizes for marketing copy: `text-fluid-hero`,
`text-fluid-h1`, `text-fluid-h2` — clamp-based, no media queries.

## Font Weights

| Name | Weight |
| --- | --- |
| Normal | 400 |
| Medium | 500 |
| Semibold | 600 |
| Bold | 700 |
| Black | 900 |

## Line Heights

| Name | Value |
| --- | --- |
| `tight` | 1.25 |
| `normal` | 1.5 |
| `relaxed` | 1.75 |

## Eyebrow component

The recurring all-caps section label pattern (formerly inlined as
`text-[11px] font-semibold uppercase tracking-[0.14em] text-accent` across
4 sites in the live app) is now centralized in
`app/components/ui/Eyebrow.tsx` (Wave G).

Visual spec:

- 11px font-size
- 600 font-weight (semibold)
- uppercase
- 0.14em letter-spacing
- `text-accent` color (engine-driven oklch via Wave B's `--accent` override)
- inherits the depth-fade transition from `.text-accent`

```tsx
import { Eyebrow } from '@/components/ui/Eyebrow';

<Eyebrow>Selected Work</Eyebrow>
```

For animated eyebrow entrances (HeroSection), wrap the component in a
`motion.div` rather than reaching for `motion.p` directly — keeps the
typography pattern centralized and the motion concern layered on top.

## Hero italic accent pattern

The Nectar hero pattern (post-PR #506) uses a Playfair italic emphasis
span inside a Google Sans Flex Black headline:

> A designer. *Learning the engineering* that ships design.

The italic span:

```tsx
<span className="font-serif italic font-medium text-accent">
  Learning the engineering
</span>
```

`font-serif` resolves to Playfair Display via the `@theme` mapping;
`text-accent` paints from the engine's `--accent` oklch (Heat × Depth
reactive). Use the same pattern when a marketing surface needs an
emphasis word.
