import { Meta } from '@storybook/addon-docs/blocks';
import { TypeRamp, TokenTable } from '../components';

<Meta title="Tokens/Typography" />

# Typography

The type scale (Major Second, 1.125). Specimens render **live** at each token's
size, so the ramp always matches the build.

## Scale

<TypeRamp />

## Tokens

<TokenTable prefix="--map-fontSize" showVar />

<TokenTable prefix="--map-lineHeight" showVar />
